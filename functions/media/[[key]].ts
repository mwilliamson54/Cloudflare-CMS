type Context = { env: { CMS_MEDIA: any }; params: { key?: string[] | string }; request: Request };
export const onRequestGet = async ({ env, params }: Context) => { const key = Array.isArray(params.key) ? params.key.join("/") : params.key || ""; const object = await env.CMS_MEDIA.get(key); if (!object) return new Response("Not found", { status: 404 }); const headers = new Headers(); object.writeHttpMetadata(headers); headers.set("etag", object.httpEtag); headers.set("cache-control", "public, max-age=31536000, immutable"); return new Response(object.body, { headers }); };
